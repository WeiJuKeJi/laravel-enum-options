<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 响应格式配置
    |--------------------------------------------------------------------------
    |
    | 定义 API 响应的键名格式，可以适配不同的响应规范
    |
    */
    'response_format' => [
        'code_key' => 'code',
        'message_key' => 'msg',
        'data_key' => 'data',
    ],

    /*
    |--------------------------------------------------------------------------
    | 颜色方案
    |--------------------------------------------------------------------------
    |
    | 选择前端 UI 库的颜色方案
    | 支持: element-plus, ant-design, tailwind, bootstrap
    |
    */
    'color_scheme' => 'element-plus',

    /*
    |--------------------------------------------------------------------------
    | 颜色映射
    |--------------------------------------------------------------------------
    |
    | 不同 UI 库的颜色映射表
    |
    */
    'color_maps' => [
        'element-plus' => [
            'primary' => 'primary',
            'success' => 'success',
            'warning' => 'warning',
            'danger' => 'danger',
            'info' => 'info',
        ],
        'ant-design' => [
            'primary' => 'blue',
            'success' => 'green',
            'warning' => 'orange',
            'danger' => 'red',
            'info' => 'blue',
        ],
        'tailwind' => [
            'primary' => 'blue',
            'success' => 'green',
            'warning' => 'yellow',
            'danger' => 'red',
            'info' => 'blue',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 标签覆盖
    |--------------------------------------------------------------------------
    |
    | 覆盖预设枚举的标签文本，无需修改枚举类
    |
    */
    'label_overrides' => [
        // 'payment_method' => [
        //     'wechat' => '微信',
        //     'alipay' => '支付宝',
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 颜色覆盖
    |--------------------------------------------------------------------------
    |
    | 覆盖预设枚举的颜色，无需修改枚举类
    |
    */
    'color_overrides' => [
        // 'payment_method' => [
        //     'wechat' => 'green',
        //     'alipay' => 'blue',
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 降级处理配置
    |--------------------------------------------------------------------------
    |
    | 当枚举值无效时的降级处理策略
    | toArraySafe() 方法会使用这些配置
    |
    */

    // 降级时使用的颜色
    'fallback_color' => 'default',

    // 降级标签的转换策略
    // 选项: none, upper, lower, ucfirst, ucwords
    'fallback_label_transform' => 'none',

    // 为特定的无效值自定义降级标签
    'fallback_labels' => [
        // 'unknown_value' => '未知值',
        // 'legacy_code' => '历史数据',
    ],

    /*
    |--------------------------------------------------------------------------
    | 自动注册路由
    |--------------------------------------------------------------------------
    |
    | 是否自动注册枚举选项的 API 路由
    | 开启后，包会自动注册所有枚举 API 路由
    |
    */
    'auto_register_routes' => true,

    /*
    |--------------------------------------------------------------------------
    | 路由配置
    |--------------------------------------------------------------------------
    |
    | 当 auto_register_routes 为 true 时生效
    |
    */
    'route_prefix' => 'api/enums',
    'route_middleware' => ['api'],  // 可根据需要添加 'auth:sanctum'
    'route_name_prefix' => 'enums',

    /*
    |--------------------------------------------------------------------------
    | 注册的枚举
    |--------------------------------------------------------------------------
    |
    | 注册可通过 API 访问的枚举类
    | 支持内置预设枚举和自定义枚举
    |
    | 格式：
    | 'key' => [
    |     'class' => EnumClass::class,        // 枚举类
    |     'name' => '显示名称',                // 显示名称
    |     'description' => '描述',             // 描述
    |     'route' => '/enums/custom-route',   // API 路由（可选，默认使用 key）
    |     'category' => 'category_name',      // 分类（可选）
    | ]
    |
    */
    'registered_enums' => [
        // 内置预设枚举（自动加载，无需手动配置）
        // 如需覆盖内置枚举的配置，可在此添加同名 key

        // 自定义枚举示例：
        // 'custom_status' => [
        //     'class' => \App\Enums\CustomStatusEnum::class,
        //     'name' => '自定义状态',
        //     'description' => '自定义的状态枚举',
        //     'route' => '/enums/custom-status',
        //     'category' => 'custom',
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 自动发现预设枚举
    |--------------------------------------------------------------------------
    |
    | 是否自动扫描和加载 src/Presets 目录下的所有内置枚举
    | 如果关闭，只使用 registered_enums 中手动注册的枚举
    |
    */
    'auto_discover_presets' => true,

    /*
    |--------------------------------------------------------------------------
    | 自动发现应用枚举
    |--------------------------------------------------------------------------
    |
    | 是否自动扫描和加载应用中的自定义枚举
    | 开启后会扫描 app/Enums 目录下所有使用 EnumOptions trait 的枚举类
    |
    */
    'auto_discover_app_enums' => true,

    /*
    |--------------------------------------------------------------------------
    | 应用枚举目录
    |--------------------------------------------------------------------------
    |
    | 自定义枚举所在的目录路径
    | 可以是单个路径或多个路径的数组
    |
    */
    'app_enums_paths' => [
        app_path('Enums'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 应用枚举命名空间
    |--------------------------------------------------------------------------
    |
    | 应用枚举的基础命名空间
    |
    */
    'app_enums_namespace' => 'App\\Enums',

    /*
    |--------------------------------------------------------------------------
    | TypeScript 导出配置
    |--------------------------------------------------------------------------
    |
    | 配置 TypeScript 类型定义的导出
    |
    */
    'typescript' => [
        'enabled' => false,
        'output_path' => resource_path('js/types/enums.ts'),
        'interface_prefix' => 'I',
        'const_prefix' => '',
    ],
];
